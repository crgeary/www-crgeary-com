import themeSwitch from './src/theme-switch';
import focusMarker from './src/focus-marker';

import '../sass/main.scss';

document.addEventListener('DOMContentLoaded', () => {
    themeSwitch();
    focusMarker();
});
